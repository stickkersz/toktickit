// Placeholder for the My Tickets screen (ui-spec.md §7). Real content is a
// separate Lab 2 Issue; this exists so the routing gate has somewhere to land.
export default function MyTickets() {
  return <p>My Tickets — coming soon.</p>;
}
